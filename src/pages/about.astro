---
import MainLayout from "../layout/MainLayout.astro";
const number = [1,2,3,4,5,999]
---

<MainLayout title={"About"}>
		<h1>About</h1>
        {number.map((n , i) => (
            <p>{n}</p>
        ))}
</MainLayout>